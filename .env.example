# BAMS Admin Environment Configuration
# Copy this to .env.local and configure as needed

# API Base URL (default: proxied through Vite)
VITE_API_URL=https://bamsserver.vercel.app

# Enable debug mode
VITE_DEBUG=true

# Map tile provider (default: OpenStreetMap)
# VITE_MAP_TILES=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
